// Configurar o valor do padding de acordo com o desejado
export const INPUT_PADDING = '1rem'

// Configurar os valores para o tamanho da fonte
export const placeholderContainerVariants = {
  open: { top: '50%', translateY: '-50%' },
  closed: {
    top: '30%',
    translateY: '-50%'
  }
}

export const textContainerVariants = {
  open: { scale: 1 },
  closed: {
    scale: 0.8,
    left: -8
  }
}
